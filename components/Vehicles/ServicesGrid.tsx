import {
  Box,
  Card,
  CardActionArea,
  CardContent,
  CardHeader,
  CardMedia,
  Grid,
  IconButton,
  List,
  ListItem,
  Menu,
  MenuItem,
  Typography,
} from "@mui/material";
// import { DataGrid } from '@mui/x-data'

interface ServicesGridProps {
  children?: React.ReactNode[];
}

export default async function ServicesGrid(props: ServicesGridProps) {
  return <Box></Box>;
}
